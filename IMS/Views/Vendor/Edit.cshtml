@model IMS.Models.Vendor
@{
    ViewData["Title"] = "Edit Vendor";
}

<div class="page-heading">
    <div class="page-title mb-4 d-flex justify-content-between align-items-center">
        <div>
            <h3>Edit Vendor</h3>
            <p class="text-subtitle text-muted mb-0">Update vendor information below</p>
        </div>
        <a asp-action="Index" class="btn btn-secondary">
            <i class="bi bi-arrow-left-circle"></i> Back to List
        </a>
    </div>

    <section class="section">
        <div class="card">
            <div class="card-header">
                <h5>Edit Vendor</h5>
            </div>
            <div class="card-body">
                <form asp-action="Edit" method="post">
                    <input type="hidden" asp-for="VendorId" />

                    <div class="mb-3">
                        <label asp-for="VendorName" class="form-label"></label>
                        <input asp-for="VendorName" class="form-control" required />
                        <span asp-validation-for="VendorName" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="ContactNo" class="form-label"></label>
                        <input asp-for="ContactNo" class="form-control" required />
                        <span asp-validation-for="ContactNo" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Address" class="form-label"></label>
                        <textarea asp-for="Address" class="form-control" rows="3" required></textarea>
                        <span asp-validation-for="Address" class="text-danger"></span>
                    </div>

                    <div class="text-end">
                        <button type="submit" class="btn btn-success">
                            <i class="bi bi-pencil-square"></i> Update
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </section>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
